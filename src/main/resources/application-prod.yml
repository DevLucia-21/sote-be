spring:
  security:
    debug: false

  jackson:
    time-zone: Asia/Seoul
    mvc:
      timezone: Asia/Seoul

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
      jdbc:
        time_zone: Asia/Seoul

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  data:
    redis:
      url: ${REDIS_URL}

  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  cloud:
    gcp:
      storage:
        project-id: sote-push
        bucket: sote-profile-image
        credentials:
          location: file:/app/config/gcp-profile.json
        auto-create-files: false

      ocr-storage:
        project-id: sote-push
        bucket: sote-diary-uploads-2025
        credentials:
          location: file:/app/config/gcp-ocr.json

ai:
  analysis:
    base-url: ${AI_ANALYSIS_BASE_URL:https://sote-ai.onrender.com}
    endpoint: ${AI_ANALYSIS_ENDPOINT:/api/analysis/result}
    timeout-seconds: ${AI_ANALYSIS_TIMEOUT:60}

fastapi:
  stt-url: ${FASTAPI_STT_URL:https://sote-ai.onrender.com/ai/stt/transcribe}
  ocr:
    url: ${FASTAPI_OCR_URL:https://sote-ai.onrender.com/ocr/preview}

jwt:
  secret: ${JWT_SECRET}
  access-expiration-ms: ${JWT_ACCESS_EXPIRE:3600000}
  refresh-expiration-ms: ${JWT_REFRESH_EXPIRE:86400000}

spotify:
  client-id: ${SPOTIFY_CLIENT_ID}
  client-secret: ${SPOTIFY_CLIENT_SECRET}

gcp:
  vision:
    credentials:
      location: file:/app/config/gcp-ocr.json

  firebase:
    credentials:
      location: file:/app/config/firebase-service-account.json

logging:
  level:
    root: INFO
    com.fluxion.sote: DEBUG
    org.springframework.web: INFO
    org.flywaydb: INFO
    org.springframework.security: INFO
